apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
spec:
  rules:
  - host: "myapp.com"
    http:
      paths:
      - path: /``
        pathType: Prefix
        backend:
          service:
            name: event-service
            port:
              number: 3000